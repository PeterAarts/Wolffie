{
  "groups": [
    {
      "title": "SolarEdge ModBus TCP Configuratie",
      "version":"1.0.1",
      "sections": [
        {
          "fields": [
            { "key": "enabled", "component": "boolean", "label": "ModBus TCP Inschakelen", "cols": 2 }
          ]
        },
        {
          "title": "Netwerk Instellingen",
          "description": "De netwerkgegevens van je SolarEdge SE3000H omvormer",
          "fields": [
            { "key": "host", "component": "text", "label": "IP Adres", "cols": 3, "placeholder": "e.g. 192.168.1.100", "required": true },
            { "key": "port", "component": "number", "label": "Poort", "cols": 3, "default": 1502, "required": true }
          ]
        },
        {
          "title": "Modbus Protocol",
          "description": "Specifieke SunSpec protocol instellingen voor SolarEdge",
          "fields": [
            { "key": "unit_id", "component": "number", "label": "Unit ID (Slave ID)", "cols": 2, "default": 1, "required": true },
            { "key": "poll_interval", "component": "number", "label": "Poll Interval (ms)", "cols": 2, "default": 10000, "suffix": " ms" },
            { "key": "timeout", "component": "number", "label": "Timeout (ms)", "cols": 2, "default": 5000, "suffix": " ms" }
          ]
        }
      ]
    }
  ],
  "actions": [
    { 
      "id": "test_modbus", 
      "label": "Test Verbinding", 
      "icon": "pi-refresh", 
      "endpoint": "/api/solaredge-modbus/test" 
    }
  ]
}